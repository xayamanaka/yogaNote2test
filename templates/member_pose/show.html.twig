{% extends 'base.html.twig' %}

{% block title %}MemberPose{% endblock %}

{% block body %}
    <h1>MemberPose</h1>

    <table class="table">
        <img src="{{ asset('uploads/'~ member_pose.memberPoseImage) }}" width="100" height="100">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ member_pose.id }}</td>
            </tr>
            <tr>
                <th>Member_pose_name</th>
                <td>{{ member_pose.memberPoseName }}</td>
            </tr>
            <tr>
                <th>Member_pose_memo</th>
                <td>{{ member_pose.memberPoseMemo }}</td>
            </tr>
            <tr>
                <th>Member_name</th>
                <td>{{ member_pose.memberName }}</td>
            </tr>
            <tr>
                <th>Member_pose_advice</th>
                <td>{{ member_pose.memberPoseAdvice }}</td>
            </tr>
            <tr>
                <th>Member_pose_image</th>
                <td>{{ member_pose.memberPoseImage }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_member_pose_index') }}">back to list</a>

    <a href="{{ path('app_member_pose_edit', {'id': member_pose.id}) }}">edit</a>

    {{ include('member_pose/_delete_form.html.twig') }}
{% endblock %}